import { CustomTaskSummarySortingPipe } from './custom-task-summary-sorting.pipe';

describe('CustomTaskSummarySortingPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomTaskSummarySortingPipe();
    expect(pipe).toBeTruthy();
  });
});
